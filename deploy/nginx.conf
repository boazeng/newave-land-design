server {
    listen 80;
    server_name newave.co.il www.newave.co.il;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # Frontend static files
    root /opt/newave-land-design/frontend/dist;
    index index.html;

    # API proxy
    location /api/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        client_max_body_size 50M;
    }

    # SPA fallback - all routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS server (uncomment after certbot setup)
# server {
#     listen 443 ssl;
#     server_name newave.co.il www.newave.co.il;
#
#     ssl_certificate /etc/letsencrypt/live/newave.co.il/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/newave.co.il/privkey.pem;
#
#     root /opt/newave-land-design/frontend/dist;
#     index index.html;
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         client_max_body_size 50M;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /assets/ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
